import { DateFormatCustomPipePipePipe } from './date-format-custom-pipe-pipe.pipe';

describe('DateFormatCustomPipePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatCustomPipePipePipe();
    expect(pipe).toBeTruthy();
  });
});
