trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  buildConfiguration: 'Release'
  webAppName: 'IoTUI'
  iisSiteName: 'IoTUI'
  serverAddress: '98.70.42.23'
  username: 'IBuser'
  password: 'IBtesting#321'

stages:
- stage: Build
  displayName: 'Build and Publish'
  jobs:
  - job: Build
    displayName: 'Build'
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '12.x'
      displayName: 'Install Node.js'

    - script: |
        npm install
        npm run build
      displayName: 'Install and Build'

    - task: PublishBuildArtifacts@1
      displayName: 'Publish Artifact'
      inputs:
        pathtoPublish: '$(System.DefaultWorkingDirectory)'
        artifactName: 'drop'
        publishLocation: 'Container'

- stage: Deploy
  displayName: 'Deploy to IIS'
  jobs:
  - job: Deploy
    displayName: 'Deploy'
    pool:
      vmImage: 'windows-latest'
    steps:
    - task: DownloadBuildArtifacts@0
      displayName: 'Download Build Artifacts'
      inputs:
        buildType: 'current'
        downloadType: 'single'
        artifactName: 'drop'
        downloadPath: '$(System.ArtifactsDirectory)'

    - task: IISWebAppDeploymentOnMachineGroup@0
      displayName: 'Deploy to IIS'
      inputs:
        WebSiteName: '$(iisSiteName)'
        VirtualApplication: '/'
        Package: '$(System.ArtifactsDirectory)/drop'
        RemoveAdditionalFilesFlag: true

    - task: IISWebAppManagementOnMachineGroup@0
      displayName: 'Stop IIS App Pool'
      inputs:
        IISDeploymentType: 'IISApplicationPool'
        ActionIISApplicationPool: 'Stop'
        StartStopRecycleAppPoolName: '$(IoTUI)'
        StartupType: 'Automatic'

    - task: IISWebAppManagementOnMachineGroup@0
      displayName: 'Start IIS App Pool'
      inputs:
        IISDeploymentType: 'IISApplicationPool'
        ActionIISApplicationPool: 'Start'
        StartStopRecycleAppPoolName: '$(IOTUI)'
        StartupType: 'Automatic'
