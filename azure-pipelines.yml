trigger:
- main  

pool:
  name: 'Azure-local'

stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - checkout: self
    - task: Npm@1
      inputs:
       command: 'install'
    
    - task: Npm@1
      inputs:
       command: 'publish'

